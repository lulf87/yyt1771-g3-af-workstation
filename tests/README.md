# Repository-level tests

This directory holds cross-cutting repository checks for G3 milestones.

Backend-specific tests live in `backend/tests/`.
Frontend end-to-end tests live in `frontend/tests/`.
