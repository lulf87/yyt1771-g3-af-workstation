# G3 离线素材登记说明 v0.1

本文件记录 G3 开发必须使用的两组真实离线素材。Codex 在开发 Offline playback、Live offline run、整体外包络 detector、A/B 稳定策略、temperature-distance 曲线和真实浏览器复测时，必须使用这些 dataset id，而不是在代码里硬编码路径。

> `golden_a_*` / `golden_c_*` 是不可变历史 ID，不再表示当前产品分类。当前新建测量统一使用 `WHOLE_ENVELOPE + ContrastWidestSpanDetector + max_width`；旧分类和 detector 只作为 `legacy_profile` 元数据保留。

---

## 1. 配置文件位置

本机实际配置文件：

```text
configs/local/offline_datasets.local.json
```

示例模板：

```text
configs/examples/offline_datasets.local.example.json
```

`configs/local/offline_datasets.local.json` 可以包含开发者本机绝对路径，因此一般不建议提交到公开仓库；但在本地 Codex 开发环境中必须存在。

---

## 2. 离线素材根目录

```text
/Users/lulingfeng/Documents/工作/开发/奥氏体变换/1771/yyt1771_starter/examples/runtime/camera_captures
```

每个数据目录下包含：

```text
frames/              每帧 npy 文件，用户说明约 1s 10 帧
manifest.json        采集 manifest / 帧 metadata / camera_meta / temperature metadata
temperature.csv      温度记录
capture.avi          如存在，用于辅助查看，不作为正式分析输入
```

---

## 3. 历史来源素材 1：golden_a_20260522_dev_lab

```text
Dataset ID: golden_a_20260522_dev_lab
Object class: WHOLE_ENVELOPE
Default detector: ContrastWidestSpanDetector
Width mode: max_width
Legacy profile: A_BALLOON_ENVELOPE + BalloonEnvelopeDetector
Path: /Users/lulingfeng/Documents/工作/开发/奥氏体变换/1771/yyt1771_starter/examples/runtime/camera_captures/20260522-183158-dev_lab
```

用途：

```text
1. 统一整体外包络与 max_width A/B 提取验证。
2. 窄测量带亮暗对比阈值和外侧跨度验证。
3. 内部网眼、纹理、暗线、交叉线忽略规则验证。
4. 夹子、连接丝、支撑丝、窄尾不决定 max_width 的验证。
5. temperature-distance 曲线验证。
6. Offline playback 和 Live offline run 真实浏览器复测。
```

ROI 规则：

```text
新建 ROI 使用默认 8 px 非零窄测量带，沿 A/B 测量方向穿过有效主体。
连接丝、夹具、焊点、固定片等非测量结构不进入测量带。
```

---

## 4. 历史来源素材 2：golden_c_20260529_dev_lab

```text
Dataset ID: golden_c_20260529_dev_lab
Object class: WHOLE_ENVELOPE
Default detector: ContrastWidestSpanDetector
Width mode: max_width
Legacy profile: C_BUNDLE_ENVELOPE + BundleEnvelopeDetector
Path: /Users/lulingfeng/Documents/工作/开发/奥氏体变换/1771/yyt1771_starter/examples/runtime/camera_captures/20260529-194304-dev_lab
```

用途：

```text
1. 统一整体外包络识别。
2. 多根细支视为一个整体目标。
3. 相邻细支之间的白色间隙视为目标内部。
4. 禁止测单根线或单根细支宽度。
5. max-width A/B 点稳定性验证。
6. temperature-distance 曲线验证。
7. Offline playback 和 Live offline run 真实浏览器复测。
```

ROI 规则与素材 1 相同：使用默认 8 px 非零窄测量带，并可按实际主体方向移动、缩放和旋转。

---

## 5. Codex 使用规则

Codex 必须遵守：

```text
1. 不要在 backend/frontend 代码中硬编码上述绝对路径。
2. 统一从 configs/local/offline_datasets.local.json 读取 dataset registry。
3. API 和 UI 应使用 dataset id，例如 golden_a_20260522_dev_lab。
4. 若配置文件不存在，应提示用户复制 configs/examples/offline_datasets.local.example.json 到 configs/local/offline_datasets.local.json 并修改路径。
5. 如果 dataset 路径不可访问，必须登记到 problem.md。
6. 所有整体外包络 detector 或窄测量带重要修复完成后，必须同时用两个 dataset 做真实浏览器复测。
```

---

## 6. 最小读取逻辑要求

后端 dataset loader 至少需要支持：

```text
load_dataset_registry(config_path)
list_offline_datasets()
resolve_dataset(dataset_id)
load_manifest(dataset_id)
load_frame(dataset_id, frame_index)
load_temperature_csv(dataset_id)
```

其中 `resolve_dataset(dataset_id)` 返回：

```text
root_path
manifest_path
temperature_csv_path
frames_dir
frame_glob
object_class
default_detector
default_width_mode
legacy_profile（可选，仅用于历史来源与兼容审计）
```

---

## 7. 验收要求

```text
1. Setup / Playback 页面可以列出 golden_a_20260522_dev_lab 和 golden_c_20260529_dev_lab。
2. 选择任一 dataset 时，新建测量默认使用 WHOLE_ENVELOPE + ContrastWidestSpanDetector + max_width。
3. 新建测量区域默认为 8 px 非零窄测量带；旧 measurement definition 仍按历史值读取。
4. 能加载 first frame / last frame。
5. 能读取 manifest.json 中的 frame timestamp、camera_meta 和 temperature metadata。
6. 能读取 temperature.csv。
7. Live offline run 能按 5-10 fps 顺序播放并记录结果。
8. 真实浏览器复测记录必须写入 problem.md 对应条目。
```
