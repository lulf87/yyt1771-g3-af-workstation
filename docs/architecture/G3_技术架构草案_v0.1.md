# G3 技术架构草案 v0.1

## 1. 总体架构

```text
frontend: Vite + React + TypeScript
backend: FastAPI + Python 3.11+
vision: pure Python / OpenCV / NumPy
services: setup / playback / run / analysis / export
storage: project / run / artifact / manifest
device adapters: camera / temperature lazy import
```

---

## 2. 后端建议目录

```text
backend/src/yyt1771_g3/
  api/
    health.py
    setup.py
    playback.py
    run.py
    analysis.py
    export.py
    camera.py
    temperature.py
  core/
    models.py
    roi.py
    coordinates.py
    config.py
    enums.py
  vision/
    detectors.py
    balloon_envelope.py
    bundle_envelope.py
    envelope_mask.py
    ab_candidates.py
    stability.py
    overlays.py
  services/
    setup_service.py
    playback_service.py
    live_offline_run_service.py
    real_camera_run_service.py
    region_detection_service.py
    analysis_service.py
    export_service.py
  storage/
    run_store.py
    artifact_store.py
    manifest_io.py
  camera/
    base.py
    offline_source.py
    hik_mvs_source.py
  temperature/
    base.py
    sync.py
    lu92xx_modbus.py
  report/
    csv_export.py
    json_export.py
    png_curves.py
```

---

## 3. 前端建议目录

```text
frontend/src/
  pages/
    SetupPage.tsx
    RunPage.tsx
    PlaybackPage.tsx
    AnalysisExportPage.tsx
  components/
    FrameCanvas.tsx
    RotatedROIEditor.tsx
    ABOverlay.tsx
    DetectorPanel.tsx
    DiagnosticsPanel.tsx
    CurveView.tsx
  api/
    client.ts
    setup.ts
    playback.ts
    run.ts
    analysis.ts
    export.ts
  geometry/
    roi.ts
    coordinates.ts
  run/
    liveOfflineRun.ts
  playback/
    playbackState.ts
  analysis/
    curves.ts
  measurementRegions.ts
  multiRegionAnalysis.ts
```

---

## 4. 坐标边界

```text
前端显示坐标：只用于渲染和鼠标交互。
measurement coordinates：正式 ROI、A/B、distance 的唯一坐标体系。
默认 measurement coordinates：原始图像 pixel 坐标。
```

前端必须把显示坐标映射到 measurement coordinates 后提交给后端。

---

## 5. 服务边界

```text
API layer：薄层，只做请求/响应。
Service layer：业务流程。
Vision layer：纯算法。
Storage layer：读写 artifact。
Camera/temperature adapters：设备隔离，lazy import。
```

---

## 6. 关键设计约束

```text
前端不计算正式 A/B / distance。
Detector 不依赖浏览器显示坐标。
无相机也可运行 offline/playback。
历史 run 不被 re-analysis 覆盖。
```

---

## 7. 多检测位置增量兼容架构

```text
MeasurementDefinition.regions（权威，1–6 个）
  -> 每帧只采集一次图像
  -> 每帧只读取一次温度
  -> region_detection_service 按 enabled region 裁剪 ROI 并独立检测
  -> candidate / temporal / policy / outlier / curve 状态全部按 region_id 隔离
  -> frame event.region_results
  -> RunManifest.region_detection_results
  -> AnalysisResult.regions（逐位置 AFAS）
  -> long / wide / per-region / combined export
```

旧接口保留第二层兼容镜像：`measurement.roi`、frame event 顶层 `detection_result/curve_points/live_point_status`、`RunManifest.detection_results` 和 `AnalysisResult` 顶层曲线/AFAS 字段均代表第一个启用位置。旧 manifest/export/import 在边界层规范化为 `region_1`，核心流程不再依赖“固定六个字段”。

前端位置配置使用 `MeasurementRegion[]`，实时状态与曲线使用以 `region_id` 为键的 map。Operator Mode 固定 `contrast_widest_span` 并只暴露共享对比度阈值、最大跳变与位置/ROI 控件；Engineering Mode 的既有单 ROI 入口通过兼容层继续工作。
